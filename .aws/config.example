# AWS CLI config for SSO (IAM Identity Center)
# Copy this file to ~/.aws/config (or append the [profile ...] block to your existing ~/.aws/config).
# Then fill in the placeholders and run: aws sso login --profile adonev-login

[profile adonev-login]
sso_start_url = https://your-org.awsapps.com/start
sso_region = eu-central-1
sso_account_id = YOUR_ACCOUNT_ID
sso_role_name = YOUR_ROLE_NAME
region = eu-central-1
output = json
